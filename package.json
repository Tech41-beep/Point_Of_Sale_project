{
  "name": "pos-project-workspace",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "built": "npm run build",
    "db:seeder": "npm --prefix backend run db:seeder",
    "postinstall": "npm --prefix backend ci --ignore-scripts",
    "start": "npm --prefix backend start"
  },
  "dependencies": {
    "@google/genai": "^2.19.0",
    "@zxing/browser": "^0.2.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "helmet": "^8.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.7.0",
    "react-router-dom": "^6.22.0"
  }
}
